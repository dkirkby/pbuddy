[project]
name = "pbva-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pbva-core",
    "pbva-db",
    "pbva-pipeline",
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
    "python-multipart>=0.0.9",
    "orjson>=3.10",
]
